# Pinned direct dependencies (generated by Copilot on 2026-01-03)
# NOTE: This pins direct dependencies only. To produce a fully reproducible lock (including transitive deps and secure hashes), run:
#   pip install pip-tools
#   pip-compile requirements.in --output-file=requirements.txt --generate-hashes
# and commit the resulting requirements.txt. Resolve with the target Python interpreter you use (e.g., python3.11).

pandas==2.1.3
requests==2.31.0
